package org.firstinspires.ftc.teamcode.enums;

public enum StartPosition { // Front autonomous is currently the same for both alliances and only needs one enum
    BACKSTAGE_BLUE, BACKSTAGE_RED, FRONT
}
